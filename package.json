{
  "name": "xylem",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync:legacy": "mkdir -p public/legacy && cp -R legacy/. public/legacy/",
    "og:capture": "node scripts/capture-og.mjs",
    "dev": "npm run sync:legacy && vite",
    "build": "tsc -b && vite build && cp -R legacy dist/legacy",
    "preview": "vite preview"
  },
  "dependencies": {
    "@chenglou/pretext": "^0.0.7",
    "@vercel/functions": "^3.6.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.2"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.5.2",
    "playwright": "^1.52.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
